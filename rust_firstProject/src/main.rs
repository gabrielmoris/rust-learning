// To start:
// > cargo new <app name>
// It creates all the necessary files
//to put the dependencies inCargo.toml ==> https://crates.io/

#![allow(unused)]
use rand::Rng;
use std::cmp::Ordering;
use std::fs::File;
use std::io;
use std::io::{BufRead, BufReader, ErrorKind, Write};

fn main() {
    println!("Hello World!");
    println!();
    println!("hello ");
    println!("format {} arguments{}", "some", 2 + 2);
}
